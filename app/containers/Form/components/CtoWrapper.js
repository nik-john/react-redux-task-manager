import styled from 'styled-components';

const CtoWrapper = styled.form`
    display: flex;
    flex-direction: column;
    flex: 10;
    align-items: stretch;
    justify-content: space-around;    
`;

export default CtoWrapper;
